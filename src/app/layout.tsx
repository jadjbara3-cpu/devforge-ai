import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DevForge AI — Full-Stack AI Developer Hub",
  description:
    "An integrated developer workspace combining LLM chat, vision analysis, image generation, voice synthesis, speech recognition, web intelligence, a code snippet vault, and a real-time collaborative task board.",
  keywords: [
    "DevForge",
    "AI",
    "Next.js",
    "TypeScript",
    "LLM",
    "VLM",
    "TTS",
    "ASR",
    "Developer Tools",
  ],
  authors: [{ name: "DevForge AI" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
